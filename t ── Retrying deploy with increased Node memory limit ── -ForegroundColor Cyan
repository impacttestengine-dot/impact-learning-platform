[33mb950504[m[33m ([m[1;36mHEAD[m[33m -> [m[1;32mmain[m[33m, [m[1;31morigin/main[m[33m, [m[1;31morigin/HEAD[m[33m)[m Elevate Live Classroom lobby design: removed all emoji, replaced with refined line-icon SVGs, restructured to a two-panel split-card layout with tighter typography and more restrained color palette (white/monochrome primary action instead of colored fills), quieter secondary links row at the bottom instead of decorated bullet-separated text. All existing create/join functionality preserved exactly.
[33m04a522f[m Add Present Lesson feature to Live Classroom: host can open a saved Lesson Plan inside the room, see every block, and click Show to Class on any block (image, video, text, header, question) - it appears as a large centered overlay for every participant while video tiles shrink to a side strip, matching Google Meet's present-content pattern but native (no screen sharing needed). Host-only controls, real-time sync via Firestore, Stop Presenting returns to normal grid layout instantly.
[33md0969e1[m Complete rebuild of billing.html from scratch: pure ASCII/SVG icons only (no emoji, immune to encoding corruption), global error handler catches ANY script error or unhandled rejection and displays it visibly instead of silently hanging on Loading forever, docx library now dynamically imported only when Download is actually clicked (so a slow/failed CDN can never block the page from loading), all core billing logic (isBillable, per-learner running count across full history, cutoff-based new-group detection, invoice numbering, duplicate prevention) preserved exactly as verified against the real class-logger.html data model.
